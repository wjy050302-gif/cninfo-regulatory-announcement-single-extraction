# Sample Checks

This folder contains lightweight, human-readable checks to demonstrate the evidence chain required by the course.

- `evidence_chain_example.md`: one end-to-end trace example (metadata -> pdf -> MinerU -> section -> extract -> validate -> final).

