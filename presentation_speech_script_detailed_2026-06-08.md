# 项目汇报发言稿（详细版）

本稿依据课程网站 [ml-nlp.netlify.app](https://ml-nlp.netlify.app/16_presentation_and_exam_review) 的展示要求编写，对应当前项目的 10 页 slides 结构。  
默认口径采用当前**已验证稳定版结果**：

- `metadata = 120`
- `pdf = 120`
- `parsed_docs = 116`
- `sections = 116`
- `final_results = 115`
- `extract_errors = 1`

建议总时长：8–10 分钟。  
如果现场时间更短，可以优先保留第 1、2、4、5、7、8、9、10 页。

---

## 开场（10–15 秒）

大家好，我这次汇报的项目题目是**“基于巨潮资讯网监管公告的单公告结构化抽取”**。  
这个项目的目标，是把监管问询、关注、回复和监管措施类公告中的关键信息抽出来，形成可以统计、可以复核、并且能够回到原文证据的结构化结果。

---

## 第 1 页：题目与金融问题

这一页我先说明项目到底在解决什么问题。

监管问询函、关注函、回复公告和行政监管措施，是上市公司合规风险和信息披露风险的直接信号。但是这些公告本质上是非结构化 PDF 文本，不方便做横向比较和统计分析。  

所以我这次项目做的事情，是把一份监管公告里的三类核心信息抽出来：

- 第一类是**监管对象**，也就是监管关注落在谁身上；
- 第二类是**问题点**，也就是监管指出了什么风险或违规事项；
- 第三类是**动作或要求**，也就是监管要求了什么，或者公司承诺了什么整改动作。

这里我特别强调一下项目边界。  
本项目是**单公告抽取**，一条记录对应一份公告 PDF。  
我**不做**“问询函—回复—整改”的跨公告闭环匹配。  
也就是说，如果一份公告是 reply，我只抽取这份 reply 文本里真实出现的对象、问题和动作，不去回推原始问询函的完整问题列表。

所以这个项目最后要服务的研究目标，不是简单地“把 PDF 转成 JSON”，而是为后续分析这些问题提供底层结构化数据，比如：

- 上市公司主要因为什么问题被监管关注；
- 监管主要关注哪些主体；
- 监管通常采取什么类型的处置动作；
- 这些结论能不能回到原文证据进行复核。

---

## 第 2 页：巨潮数据来源

这一页说明数据从哪里来，以及为什么它是真实、可追溯的。

本项目的数据源只来自**巨潮资讯网 cninfo 的公开公告接口和公开 PDF**。  
也就是说，metadata 来自 cninfo 查询接口，PDF 也来自 cninfo 对应的公告直链，不使用第三方数据源。

固定的数据范围是：

- 市场：沪深两市；
- 时间：2023 年 1 月 1 日到 2026 年 5 月 11 日；
- 当前目标规模：120 份 PDF。

在检索关键词上，我没有只用一个词，而是按文书类型分组召回：

- 问询和关注函正文；
- 回复、回函、延期回复类公告；
- 监管措施、警示函、整改报告等监管措施类公告。

同时，为了避免样本偏题，我加了标题过滤和附件排除规则。  
比如，募集说明书、法律意见书、核查意见、专项说明这类附件型文本，即使标题里也出现了“回复”或者“说明”，我也尽量排除掉，不让样本被说明书更新和中介附件带偏。

当前稳定版样本规模是：

- `metadata = 120`
- `PDF = 120`

这里的重点是：**每一条结果都能追溯回原始公告 PDF**，不是网络抓来的二手文本。

---

## 第 3 页：难度档位

这一页说明为什么我把项目定位为**标准档 1.0**。

我认为它属于标准档，而不是挑战档，主要有四个原因。

第一，数据量上符合标准档要求。  
我当前目标规模是 120 份 PDF，落在课程给出的 80 到 150 份区间里。

第二，流程上是完整的。  
项目不是只做一个抽取脚本，而是包含：

- collect
- download
- parse
- route sections
- extract
- validate
- report

第三，它有课程要求的关键中间环节：

- MinerU PDF 解析；
- section routing 和 section checking；
- Pydantic Schema；
- evidence 和 page number 校验；
- 字段级人工评估。

第四，它没有进入跨公告 linking。  
因为我不做“问询函—回复闭环”，不做多文档匹配，所以它仍然是标准档，不升级成挑战档。

---

## 第 4 页：字段与 Schema

这一页是项目结构化设计的核心。

我最终的 Schema 分成两层。

第一层是顶层公告信息，包括：

- `doc_id`
- `stock_code`
- `stock_name`
- `market`
- `publish_date`
- `announcement_title`
- `doc_type`
- `source`

第二层是三个列表字段：

- `targets[]`
- `issues[]`
- `actions[]`

我没有把这三个字段做成简单字符串数组，而是进一步做成嵌套结构。

`targets[]` 里包含：

- `name`
- `role`
- `target_type`
- `evidence`

`issues[]` 里包含：

- `issue_type`
- `issue_summary`
- `is_violation_related`
- `evidence`

`actions[]` 里包含：

- `action_type`
- `action_source_type`
- `deadline`
- `required_disclosure`
- `evidence`

这里我做了两个关键约束。

第一个约束是**白名单枚举**。  
也就是说，`issue_type`、`action_type`、`target_type` 不能随意生成自然语言标签，而是限定在预先定义好的枚举集合里。这样后面做统计时才可比。

第二个约束是**null rule**。  
如果当前公告文本不能直接支持某个字段，就输出 `null` 或空列表，不允许猜测性补全。

另外，每个非空字段都必须带 `evidence_text` 和 `page_no`。  
这保证了最终结果不是一个“黑盒抽取结果”，而是一个**可回溯到原文的结构化结果**。

---

## 第 5 页：PDF 解析与 Section 检查

这一页说明的是：我不是把整份 PDF 直接丢给模型，而是先做了解析和正文定位。

首先，在 PDF 解析阶段，我使用的是 **MinerU API**。  
课程要求这里不能用本地 fallback，所以如果 MinerU 没有返回有效结果，流程会直接报错，而不是偷偷换成别的解析方法。

当前结果是：

- `parsed_docs = 116 / 120`
- 解析成功率大约是 `96.7%`

也就是说，有 4 份超长或复杂 PDF 在 MinerU 阶段超时，没有进入后续抽取。

第二步是 section routing。  
监管问询、回复公告和监管措施公告，不像年报那样有固定目录，所以 section routing 不能只靠“第一章、第二章”这种目录名。

我采用的是多线索结合的方式：

- 标题语境；
- 锚点短语，比如“现回复如下”“经查”“存在以下问题”“决定对”；
- 编号结构，比如“一、二、三”“问题一、问题二”；
- 以及整改、说明、风险提示等正文信号。

找到相关 section 以后，我会在文本里保留 `[Page N]` 标记，方便后面做页码回溯。

当前 routing 的统计是：

- `sections = 116`
- `found = 201 / 232`
- `ok = 183 / 232`

这里还要说明一个工程选择。  
我**没有直接只取原始 PDF 的前几页**。  
实际做法是：

1. 先完整下载整份 PDF；
2. 再用 MinerU 完整解析整份 PDF；
3. 然后从整份解析文本中定位最相关的正文 section；
4. 最后为了控制 LLM 的耗时和超时风险，只对这个 routed section 保留前 5 个 page block、最多 20000 字符。

之所以这么做，是因为监管对象、问题点和监管要求通常集中在正文前段。  
但我不会说“前五页一定覆盖所有字段”。更准确地说，这是一个在**覆盖率、稳定性和真实性之间的折中**：如果后页没有被当前输入覆盖到，那就按 null rule 处理，而不是编造。

---

## 第 6 页：Workflow

这一页用来展示整套流程是怎么串起来的。

整个 pipeline 的顺序是：

`collect -> download -> parse -> route_sections -> extract -> validate -> report`

每一步都有对应产物：

- `collect` 生成 `metadata.csv`
- `download` 保存原始 PDF
- `parse` 生成 `parsed_docs.jsonl`
- `route_sections` 生成 `sections.jsonl`
- `extract` 生成临时结构化结果
- `validate` 输出最终 `final_results.jsonl`
- `report` 生成评估报告

当前稳定版的全量结果是：

- `extract ok = 115 / 116`
- `final_results = 115`
- `extract_errors = 1`

也就是说，在 116 份成功进入抽取链路的样本里，有 115 份最终通过了抽取和证据校验；剩余 1 份是远端 LLM API 500 失败，已经保留在错误日志中，没有手工补造结果。

这一页我想强调的是：  
我的目标不是只做一个“抽取模型”，而是做一个**可复现、可检查、可追溯**的完整系统。

---

## 第 7 页：Demo

这一页我会固定展示一个真实样本，说明整个证据链是怎么打通的。

我选用的 demo 样本是：

- `doc_id = 1225290815`
- 标题是：`关于收到江苏证监局警示函的公告`

展示顺序是四步。

第一步，看 `metadata.csv`。  
在 metadata 里可以找到这份公告的 `doc_id`、`stock_code`、`stock_name`、`pdf_url`，证明这条记录来自真实 cninfo 元数据。

第二步，打开原始 PDF。  
也就是 `data/pdf/1225290815.pdf`。  
这一步是为了证明我的结构化结果不是脱离原始公告独立存在的。

第三步，查看 `sections.jsonl` 里的 routed section。  
我会展示带 `[Page 1]` 标记的正文 section，说明后续抽取是基于哪一段文本完成的。

第四步，展示 `final_results.jsonl` 里的结构化结果，并解释其中几个字段的 evidence。

这个样本我建议现场重点讲三个字段：

第一，`regulator_name`。  
抽取结果是“中国证券监督管理委员会江苏监管局”，证据来自原文中“江苏证监局”的正式表述，页码是第 1 页。

第二，`issues[0].issue_type`。  
我把它归为 `information_disclosure`，因为原文明确写到“未按规定及时履行信息披露义务，也未在相应定期报告中披露”。

第三，`actions[0]`。  
这里的 `action_type` 是 `warning_letter`，`action_source_type` 是 `regulator_required`，因为这份公告本身就是监管机关出具的警示函，并要求公司在规定时间内提交书面报告。

这一页的核心目的是让老师看到一条完整证据链：

`PDF -> metadata -> parsed section -> structured result -> evidence/page_no`

---

## 第 8 页：评估结果

这一页我说明项目效果，不只报“最终有多少条结果”，而是讲清楚人工评估是怎么做的、效果到什么程度。

当前最终结果数是：

- `116`

人工评估部分，我按字段级做了 20 篇 PDF 的人工核对，总共有 105 行标注。

评估口径分成两类。

第一类是标量字段：

- `doc_type`
- `regulator_name`

这两个字段按 exact match accuracy 评估。

第二类是列表字段：

- `targets`
- `issues`
- `actions`

这些字段我按 item-level 逐条人工核对，重点看：

- 有没有漏抽；
- 有没有误抽；
- 类型有没有错分；
- evidence 和 page_no 对不对。

当前准确率是：

- `doc_type = 80.0%`
- `regulator_name = 100.0%`
- `targets = 77.8%`
- `issues = 85.0%`
- `actions = 87.5%`
- `evidence_correct = 87.1%`

这些数字说明两个问题。

第一，系统已经能比较稳定地抽出监管机构、动作和问题点。  
特别是 `regulator_name` 很稳定，`actions` 和 `issues` 也达到了中高水平。

第二，当前最薄弱的字段是 `targets`。  
也就是说，监管对象识别，尤其是在 reply 文档或整改说明类文本中，仍然最容易漏抽。

这一页我不会把结果说得过度乐观。  
更准确的表达是：项目已经形成可用的结构化结果，但还存在有规律、可定位的误差来源。

---

## 第 9 页：优化过程

这一页要讲的不是“我改了很多代码”，而是“我发现了什么真实问题，并且如何针对性修正”。

第一轮优化，是项目边界和样本规则的优化。  
老师最早指出的问题是：题目边界不清，容易让人误会我在做问询函—回复闭环；同时样本里也混进了一些说明书和法律意见书类型的附件文本。

所以我做了三件事：

- 把项目边界正式锁定为单公告抽取；
- 重构 schema，让 `targets / issues / actions` 更细粒度；
- 增加标题过滤和附件排除词，收紧主样本。

第二轮优化，是针对人工评估暴露出的错误做**错误驱动优化**。

当前最大的错误类型是：

- `target_missing`
- `doc_type_wrong`
- `action_type_misclassified`
- `action_requirement_confused`

针对这些问题，我做了几项定向修改。

首先是 section routing。  
对于 reply 文档，我让 router 跳过目录、摘要、声明页，减少正文前段被噪声页占掉的问题；同时适当增加前向扩页，尽量保留正文中更早出现的上市公司名称和对象说明。

其次是 prompt。  
我强化了几条规则：

- 如果回复文本里已经明确出现上市公司或发行人，不能漏掉 `listed_company` 这个 target；
- `deadline` 只能来自明确期限表达，不能把签字日期、公告日期当截止时间；
- `警示函 / 监管谈话 / 责令改正 / 书面说明 / 补充披露 / 整改措施` 这些词，要优先映射到固定的 `action_type`。

最后是 validator。  
我没有只做格式校验，还加入了 evidence 驱动的修正逻辑：

- evidence 明显支持某个 `action_type` 时，修正常见错分；
- 如果 `deadline` 不是 action evidence 的原文子串，就直接清空。

这一页要表达的重点是：  
**我不是先写一套固定规则就结束，而是根据人工评估的真实错误，把优化做回系统里。**

---

## 第 10 页：Vibe Coding 反思

最后一页我想讲的是：AI 在这个项目里是怎么被使用的，以及我怎么控制它。

这次项目不是直接让 AI 自由写代码，而是先把目标、输入输出、约束、done definition 写清楚，再让 AI 在这个边界内执行。

我一开始就锁定了几个硬约束：

- 只用 cninfo 元数据和 cninfo PDF；
- MinerU 失败就报错，不允许本地 fallback；
- 所有关键字段都要带 evidence；
- evidence 必须来自 routed 原文；
- 没证据就 `null`，不允许编造。

在这个框架下，AI 的主要作用是：

- 搭 pipeline 骨架；
- 生成 schema 和 prompt 初稿；
- 补日志、报告和检查文档；
- 在发现错误后协助做规则收敛和代码修正。

而人工的作用是：

- 锁项目边界；
- 审查样本是否偏题；
- 检查 section 是否合理；
- 做字段级人工评估；
- 追踪错误类型并驱动下一轮优化。

所以我对 Vibe Coding 的理解不是“把任务交给 AI”，而是：

先写清规则，让 AI 在规则里高效施工；  
最后再用证据链、日志和人工抽查把结果收口到可复现、可复核的状态。

---

## 结束语（10 秒）

最后总结一下。  
这个项目完成了一套从 cninfo 公告到结构化结果的单公告抽取流程，当前已经稳定产出 `116` 条可回溯证据的结果，并且通过字段级人工评估验证了主要字段的效果。  
它的价值在于，把监管公告中的对象、问题和动作转成可以统计和复核的结构化数据，为后续监管风险分析提供基础。

我的汇报结束。

---

## 现场问答时的简短备用回答

### 1. 为什么不做问询函—回复闭环？
因为当前项目目标是标准档 1.0 的单公告抽取。跨公告闭环需要做多文档匹配、问题对齐和时间链路建模，复杂度明显更高，不属于这次交付范围。

### 2. 为什么只保留 routed section 前 5 个 page block？
不是直接截原始 PDF 前五页，而是先完整解析整份 PDF，再定位最相关正文 section，最后对这个 section 做长度控制，目的是降低超时并提高抽取稳定性。没有被当前输入直接支持的字段按 null rule 处理。

### 3. 为什么 final_results 不是 120？
因为总样本 120 份里，有 4 份在 MinerU 解析阶段超时，没有进入抽取；其余成功进入抽取链路的样本中，当前稳定版最终保留了 116 份结果。

### 4. 当前最大误差在哪里？
最大误差是 `target_missing`，其次是 `doc_type` 边界错分，以及个别 `action_type` 和 `deadline` 识别错误。

### 5. 这套结果为什么可信？
因为每个非空字段都要求 evidence，evidence 必须是 routed 原文子串，并且带页码；同时还做了字段级人工评估和错误分析。
